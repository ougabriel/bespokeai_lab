version = "1.0"

[metadata]
author_name = "Candidate"
author_email = "candidate@example.com"
difficulty_explanation = "This is one DevOps incident. The active prod hotfix lane is documented in the seeded runbook and reference profiles, but the seeded source-of-truth has drift across CI, registry, GitOps, promotion, observability, traffic, mesh, and telemetry. Strong scores require repairing the lane so two consecutive rollouts converge on the same prod contract."
category = "devops"
tags = ["devops", "gitops", "argocd", "ci-cd", "observability", "service-mesh", "incident-response"]

[environment.runtime]
python = "3.11"

[verifier]
timeout_sec = 360.0

[agent]
timeout_sec = 360.0

[environment]
build_timeout_sec = 900.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
