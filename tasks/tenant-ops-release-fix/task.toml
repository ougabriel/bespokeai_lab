version = "1.0"

[metadata]
author_name = "Candidate"
author_email = "candidate@example.com"
difficulty_explanation = "The agent has to trace multiple interacting failures across configuration loading, database bootstrap, API startup, and report aggregation, including cross-date deduplication, tenant-scoped job identity, and status normalization."
category = "backend-engineering"
tags = ["python", "fastapi", "sqlite", "debugging", "devops"]

[environment.runtime]
python = "3.12"

[verifier]
timeout_sec = 300.0

[agent]
timeout_sec = 300.0

[environment]
build_timeout_sec = 900.0
cpus = 2
memory_mb = 4096
storage_mb = 10240
